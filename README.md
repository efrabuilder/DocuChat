# DocuChat
Chatbot en Python que responde preguntas sobre tus propios documentos (PDF/TXT/MD) usando la API de Claude y recuperación de contexto (RAG básico con TF-IDF). Incluye versión web (Streamlit) y de escritorio (Tkinter).
